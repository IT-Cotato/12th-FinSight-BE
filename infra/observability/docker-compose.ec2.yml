services:
  prometheus-config:
    environment:
      - ENVIRONMENT=production

  prometheus:
    ports:
      - "127.0.0.1:9090:9090"
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # Grafana Cloud 사용으로 로컬 Grafana 비활성화
  grafana:
    profiles:
      - disabled
